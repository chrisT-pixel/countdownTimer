import React from 'react';
import { Container } from 'react-bootstrap';

export function MainView() {
    return(
        <Container>
            <p>This is where everything goes in.</p>
        </Container>
    );
}